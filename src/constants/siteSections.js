/**
 * Blog ?view= query keys mapped to blogData category labels.
 * Used by BlogPage when linking from external CTAs (legacy footer views removed).
 */
export const BLOG_VIEW_CATEGORIES = {
  guides: 'Invest',
  reports: 'News',
};
